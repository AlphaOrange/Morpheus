export default class ChooserAgent {
  // NPC Agent
  // This agent is for choosing which character acts next
  // Currently it does not use any AI

  constructor() {}

  // Run Action: evaluate and return the next npc to act
  run({ npcs }) {
    // TODO: this is just a mock function yet
    return npcs[0]
  }
}
